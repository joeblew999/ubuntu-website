name = "ubuntusoftware-net"
compatibility_date = "2024-01-01"

# Cloudflare Pages configuration
# Note: For Direct Upload deployment, build happens locally via Hugo
# The 'public' directory is then uploaded to Cloudflare Pages
pages_build_output_dir = "public"
