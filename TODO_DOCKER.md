# DOCKER

We need a DOCKER for XPlat

so the exact same thing on a desktop can run in a docker.

KO can do it for us i think, because we are just copying our binaries inside docker.

osgeo/gdal:ubuntu-small-3.6.3 has all the correct meta data and seems like a good example.

https://github.com/OSGeo/gdal


https://github.com/OSGeo/gdal/tree/master/docker
